# data — nothing under these is source
data/
*.geojson

# example: tiles come from the GitHub Release (python download.py --tiles <REGION>); own runs are not
# committed; Tile_Mappings.csv / imagery_info.json (input) and our results (output/provided) are
example/input/*/tiles/
example/input/*/*.zip
example/output/*
!example/output/provided/
!example/output/provided/**

# model weights: distributed through GitHub Releases (python download.py → run/src/)
run/src/*.pt

# caches (osmnx / Overpass, ultralytics)
cache/
runs/

# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# virtual environments
.venv/
venv/

# OS / editor
.DS_Store
.vscode/
.ipynb_checkpoints/
